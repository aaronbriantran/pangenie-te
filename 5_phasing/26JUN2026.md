# 26 June 2026

The workflow seems to be viable so far, but I have to see how phase_common works. One big issue is that phase_common is throwing an error for lacking samples, as it requires at least 50 samples. There are 88 haplotypes in the reference panel, which is actually only 44 samples!

I need a bigger panel, so I decided to switch to HPRCv2. That has 231 samples, or 462 haplotypes. I also need to show that the samples that we used for genotyping in HPRCv1 are present in HPRCv2, or I risk losing some variation. Using:

<code>bcftools query -l mc_filtered_ids_biallelic.vcf.gz > big_samples.txt</code>
<code>bcftools query -l ../../../final/3_pangenie/input_files/biallelic.vcf.gz > small_samples.txt</code>

I get two lists of the samples from the big HPRCv2 biallelic callset and the small HPRCv1 biallelic callset. Then, I grep the matches between them:

<code>grep -f small_samples.txt big_samples.txt > matches.txt</code>

Looking at the <code>wc -l </code> of the matches.txt and the small_samples.txt shows 43 and 44 respectively, and using:

<code>diff small_samples.txt matches.txt</code> shows that HG03492 is the only sample not present in the matches--that is, that is the one sample specific to HPRCv1 and not in HPRCv2. I'm willing to use HPRCv2, as I doubt I'll lose that much information from 2 haplotypes.
